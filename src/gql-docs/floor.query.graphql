query floor($id: String!) {
  floor(id: $id) {
    id
    name
    zones {
      id
      name
      devices {
        mac
        name
        mqtt_topic
        deviceType {
          id
          name
        }
      }
    }
  }
}
